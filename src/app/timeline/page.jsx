import TimelineClient from "@/components/TimelineData/TimelineClient";

export const metadata = {
    title: "Timeline | KeenKeeper",
    description: "Timeline of the Interactions of user",
};

const TimeLinePage = () => {

    return <TimelineClient />;
};

export default TimeLinePage;